export default function Fallback(props) {
    return(
        <div className="flex items-center justify-center">
            <p>The page could not be loaded, please try again, when you have an internet connection.</p>
        </div>
    );
}